import * as alert from './alert.action';
import * as note from './note.action';
import * as user from './user.action';

export const { alertActions } = alert;
export const { noteActions } = note;
export const { userActions } = user;
