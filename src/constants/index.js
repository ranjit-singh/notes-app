import * as alert from './alert.constants';
import * as note from './note.constants';
import * as user from './user.constants';

export const { alertConstants } = alert;
export const { noteConstants } = note;
export const { userConstants } = user;
